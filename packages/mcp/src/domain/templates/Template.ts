/**
 * Template class placeholder
 * 
 * This is a simplified version just to satisfy type requirements
 */
export class Template {
  readonly id: string;
  readonly type: string;
  
  constructor(id: string, type: string) {
    this.id = id;
    this.type = type;
  }
}
